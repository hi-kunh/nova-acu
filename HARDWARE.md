# 하드웨어 구성

이 프로젝트의 물리적 장치 구성 기록. 소프트웨어 진행 상황은 [README.md](README.md),
남은 작업은 [TODO.md](TODO.md) 참고.

- 최종 갱신: 2026-09-11
- 상태: **RRU 보드는 설계 단계** (실물 확보 전). 아래 채널 번호 체계는 확정 전 제안값
- 연결 방식: **USB, ACU 1 : RRU 여러 대** (2026-09-11 변경. 이전엔 1:1). **프로토콜 모듈 14개까지** (2026-09-11 결정)
- RRU MCU 확정: **STM32C562** (개발보드 NUCLEO-C562RE)
- 설치 형태: ACU와 RRU는 **같은 함체 안**에 들어간다 (RRU 여러 대 모두 같은 함체, 2026-09-11 확인)
- 외부 인터럽트: **STM32C562로 충분함 확인** (Wiegand 16입력 수용 가능 — 아래 "주의 3")
- **개발 진행 방식 확정**: 개발보드(NUCLEO-C562RE) 구매 -> **8모듈 중 2모듈만 구현해 검증** ->
  검증 끝난 뒤 본 제품(8모듈) 보드 설계. 상세는 아래 "개발 단계 계획" 절

---

## 전체 구성

```
  [상위 시스템 / 웹앱]
          |
          |  Ethernet - TCP / IDTi 프로토콜 V2      (5단계: 구현 완료)
          |
  +-------------------------------------------+
  |  ACU  (Access Control Unit)               |
  |  RK3566 / Radxa CM3 IO Board              |   <- 이 저장소의 acud 데몬
  |  임베디드 리눅스, headless                 |
  +-------------------------------------------+
          |
          |  USB - RRU 여러 대(허브 경유), ACU<->RRU 프로토콜   (6단계: 설계·구현 대상)
          |
  +-------------------------------------------+
  |  RRU  (Remote Reader Unit)                |
  |  리더/센서/릴레이 물리 I/O 담당             |
  +-------------------------------------------+
     |        |         |
     |        |         +-- 출력 16채널 (릴레이 8x2, 채널별 NO/NC)
     |        +------------ 입력 32채널 (Exit / Door / Lock x 8  +  알람 / 화재 x 4)
     +--------------------- 리더 8채널  (Wiegand)
```

**핵심**: 리더기·센서·릴레이는 ACU(RK3566)에 **직접 연결되지 않는다.**
Wiegand 수신과 GPIO 제어는 전부 RRU가 담당하고, ACU는 USB로 RRU와만 통신한다.
즉 ACU 쪽 6단계 작업은 "RK3566에서 Wiegand를 직접 받는 일"이 아니라
**"ACU-RRU 간 USB(CDC-ACM) 프로토콜을 설계하고 구현하는 일"** 이다.

---

## ACU 보드

| 항목 | 내용 |
|------|------|
| SoC | RK3566 |
| 보드 | Radxa CM3 IO Board (보유 중) |
| OS | 임베디드 리눅스 (headless CLI 데몬) |
| 상위 통신 | Ethernet / TCP, IDTi 프로토콜 V2 |
| 하위 통신 | **USB** -> RRU 여러 대 (가칭 `RRU-M2` / `RRU-M4`, 합계 모듈 14개까지) |
| 역할 | 출입 판정, DB(SQLite3), 설정 관리, 상위 시스템 연동, 이벤트 로그 |

### ACU <-> RRU 링크

| 항목 | 값 | 비고 |
|------|-----|------|
| 토폴로지 | **1 : N** | ACU 1대에 RRU 여러 대 (2026-09-11 변경). 합계 모듈 14개(리더 28)까지 |
| 설치 | **같은 함체 내부** | 배선 길이 문제 없음 |
| 물리 계층 | **USB (확정)** | IO 보드 J18 내부 헤더 사용 |
| 논리 계층 | 시리얼 바이트 스트림 | 물리 계층과 무관하게 동일 |
| baud / 프레이밍 | **115200 8N1** | 네이티브 CDC-ACM이라 형식적 값 |

**이 결정이 프로토콜 설계에 주는 영향**:

1. **프레임에 장치 주소 필드는 여전히 필요 없다.** USB에서는 RRU마다 별도 장치 노드가 생겨 경로 자체가
   RRU를 구분한다. 대신 **연결 직후 RRU의 DIP 스위치 ID를 받아** 어느 모듈 번호 범위인지 정한다.
   (`/dev/ttyACMn` 번호는 부팅·재연결마다 바뀔 수 있어 식별에 쓰지 않는다)
2. **RRU가 카드 이벤트를 능동적으로 먼저 보낸다.** UART(full duplex)든 USB CDC든 양방향 동시 통신이
   가능하므로 마스터-슬레이브 폴링에 묶이지 않는다. 카드 태그 즉시 ACU로 밀어 올린다.

### 물리 계층: USB (확정)

UART 경로가 핀먹스/콘솔 충돌/문서 불일치로 확실하지 않았고, ACU와 RRU가 같은 함체에 들어가므로
RS-232로 갈 이유도 없어졌다. **USB로 확정.**

**IO 보드 회로도 확인 결과 (USB)**

- USB 2.0 HUB를 통해 4포트: `USB2.0_1` / `USB2.0_2` (외부 Type-A 소켓),
  **`USB2.0_3_CON` / `USB2.0_4_CON` (내부 커넥터)**
- **`J18` = `DIP_10PIN-2_54MM`** — 2.54mm 10핀 헤더에 USB 2포트(`USB3_DP/DM`, `USB4_DP/DM`)가 나온다.
  PC 메인보드 내부 USB 헤더와 같은 형태로, **같은 함체 안 보드끼리 연결하기에 딱 맞다**
  (외부 Type-A 케이블 불필요)
- 그 외 USB 3.0 HOST1(Type-A), USB OTG0(micro USB)도 있음

**채택 근거**

- **UART에서 막혔던 문제가 전부 사라진다** — 핀먹스 오버레이 확인 불필요, 디버그 콘솔(ttyS2) 충돌 없음,
  문서 리비전 불일치 무관, RS-232 트랜시버(MAX3232) 양단 불필요
- 함체 내부 10핀 헤더로 바로 연결 가능
- **소프트웨어 영향이 사실상 없다** — ACU 입장에서는 여전히 `/dev/tty*`를 열고 읽고 쓰는 시리얼 포트다.
  프레임 구조/CRC/이벤트 통지 방식 등 프로토콜 설계는 그대로 재사용된다
  -> **되돌리기 쉬운 결정**. 나중에 UART로 바꿔도 `rru.c`의 포트 여는 부분만 손대면 된다

**주의할 점 (설계·구현에 반드시 반영)**

- **링크가 끊겼다 다시 붙을 수 있다.** UART는 선만 붙어 있으면 계속 살아 있지만, USB는 재열거
  (re-enumeration)가 일어나면 1~2초 링크가 사라진다. **출입 제어 장치는 릴레이/솔레노이드 락 같은
  유도성 부하를 스위칭하므로 EMI로 인한 USB 재열거 위험이 실재한다**
  -> 완화: 릴레이 코일에 플라이백 다이오드/스너버, 접지 설계, 짧은 배선, USB 라인과 릴레이 배선 분리
  -> **프로토콜에 반드시 반영**: RRU가 이벤트를 자체 버퍼에 쌓고 ACU가 ACK할 때까지 유지.
     링크가 잠깐 끊겨도 카드 태그가 유실되지 않아야 한다 (어차피 있어야 할 설계)
- **디바이스 노드 이름이 고정이 아니다** — `/dev/ttyUSB0`/`/dev/ttyACM0` 번호가 바뀔 수 있다
  -> **udev 규칙으로 고정 심볼릭 링크 필수** (VID/PID 또는 시리얼 번호 기준, 예: `/dev/rru`)
- **부팅 시 열거 타이밍** — acud 시작 시점에 아직 장치가 안 잡혀 있을 수 있다
  -> 없으면 재시도하는 로직 필요 (net_init 실패해도 계속 도는 기존 fail-safe 원칙과 동일)
- **USB로 RRU에 전원 공급은 불가** — USB 2.0은 500mA인데 리더 8개 + 릴레이 8개는 훨씬 넘는다.
  RRU는 자체 전원을 써야 하고, USB는 신호선(D+/D-/GND)만 쓴다. **VBUS 연결 여부와 GND 처리 주의**

**RRU 쪽 USB 구현 방식: CDC-ACM (브릿지 IC 불필요)**

RRU MCU는 **STM32C562RE (NUCLEO-C562RE 개발보드)** 로 시작한다. 보드에 네이티브 USB Type-C
Device 커넥터가 있으므로 **CDC-ACM으로 구현** -> ACU에서 `/dev/ttyACM0`.
**CP2102/FT232 같은 브릿지 IC가 필요 없다.** 상세는 아래 "RRU MCU" 절 참고

> **ACU(호스트) 쪽 준비 완료 (2026-09-08 확인)**: 보드에 올린 Radxa Debian Bullseye b25
> (커널 `5.10.160-18-rk356x`)에 `cdc-acm` 모듈이 들어 있다
> (`/lib/modules/5.10.160-18-rk356x/kernel/drivers/usb/class/cdc-acm.ko.xz`).
> 로더블 모듈이라 RRU를 꽂으면 자동 로드된다. 커널을 다시 빌드할 일은 없다..

## RRU MCU: STM32C562 (최종 확정)

**RRU의 MCU는 STM32C562로 확정.** 개발은 **NUCLEO-C562RE**(STM32C562RE 탑재) 개발보드에서 시작하고,
최종 RRU 보드는 이 위에서 프로토콜을 검증한 뒤 설계한다.

> 패키지만은 열려 있다 — `RE`는 LQFP64인데 아래 "주의 2"대로 핀이 모자란다.
> **MCU는 STM32C562로 고정하되, 최종 보드에서 같은 계열의 상위 핀수 패키지를 쓸지는 따로 결정**한다.

| 항목 | 내용 |
|------|------|
| 코어 | Arm Cortex-M33, 최대 144 MHz |
| 메모리 | Flash 512KB / SRAM 128KB |
| 패키지 | LQFP64 (`RE`의 `R` = 64핀) — 최종 보드에서는 재검토 |
| USB | **네이티브 USB Type-C, full speed Device 모드** -> CDC-ACM |
| 디버거 | 온보드 ST-LINK-V3EC (Virtual COM Port 포함) |
| 확장 | Arduino Uno V3 커넥터, ST morpho 커넥터(전체 I/O 접근) |
| 기타 | CAN FD 커넥터(CN18) 탑재, MIPI10 디버그 커넥터 |

### 이 선택이 좋은 이유

- **네이티브 USB Device 커넥터가 보드에 이미 있다** -> 브릿지 IC도, 별도 배선도 불필요.
  CDC-ACM으로 구현하면 ACU에서 `/dev/ttyACM0`으로 보인다. 앞서 정한 USB 물리 계층과 정확히 맞는다
- **ST-LINK VCP가 별도 시리얼 채널로 하나 더 생긴다** -> RRU 디버그 로그를 프로토콜 링크에 섞지 않고
  독립 채널로 뽑을 수 있다. 브링업 때 특히 유용
- **CAN FD 커넥터(CN18)가 있어 나중 RK3568 + CAN 경로도 같은 보드에서 미리 검증 가능**

### 주의 1 - USB-C 커넥터로 보드에 전원을 넣을 수 없다

dead-battery 풀다운이 없어 self-powered 모드가 전제다. USB-C to USB-C 케이블로는 급전이 안 된다.
어차피 RRU는 자체 전원을 쓸 예정이라 방향은 맞지만,
**ACU(호스트) - RRU(디바이스, 자체 전원) 연결 시 VBUS/GND 처리를 명확히 정할 것.**

### 주의 2 - I/O 핀 총량이 빠듯하다

RRU가 필요한 I/O는 **Wiegand 8채널 x (D0,D1) = 16 + 입력 32(Exit/Door/Lock 24 + 알람·화재 8) + 출력 16 = 64개**.
LQFP64의 가용 GPIO는 대략 50개 안팎이고 여기에 USB, SWD, 발진기까지 빼면 **사실상 한계선을 넘는다.**
(메모리는 512KB/128KB로 충분하다 - 부족한 건 핀이다)

### 주의 3 - Wiegand 인터럽트 핀 (해결됨: 충분함)

Wiegand는 D0/D1 각각의 하강 엣지로 비트를 받으므로 **리더 8개 x 2 = 16개의 엣지 인터럽트 입력**이
필요하다. 이것이 STM32C562로 감당 가능한지가 마지막 쟁점이었다.

**결론: STM32C562의 외부 인터럽트로 충분하다.** 16개 직결(아래 A안)로 간다.
따라서 아래 B~D안(게이트로 묶기, 타이머 Input Capture, MCU 2개)은 **채택하지 않는다.**
설계 과정 기록으로만 남긴다.

| 안 | 필요 인터럽트 | 판정 |
|----|--------------|------|
| **A. D0/D1 전부 직결** | **16** | **채택** — 인터럽트 수량 충분 확인 |
| B. 리더당 게이트(74HC08 등)로 묶기 | 8 | 미채택. 인터럽트가 모자랄 때의 대안이었음 |
| C. 타이머 Input Capture 병용 | 상황에 따라 | 미채택 |
| D. 리더 수를 나눠 MCU 2개 | 8씩 | 미채택 |

(입력 32개 - Exit/Door/Lock 24 + 알람·화재 8 - 는 느린 신호라 폴링으로 충분해서 인터럽트를 쓰지 않아도 된다)

**남은 것 — 인터럽트 개수가 아니라 핀 배치 문제**

수량은 해결됐지만, 최종 보드 설계 때 **16개 Wiegand 입력의 핀 배치**는 여전히 정해야 한다
(같은 인터럽트 라인을 공유하는 핀끼리 겹치지 않게 배치). 이건 "주의 2"의 총 I/O 부족 문제와 함께
**최종 RRU 보드 설계 단계**에서 다루고, **개발보드 단계에서는 2모듈(리더 2채널)만 쓰므로 문제되지 않는다.**

> B안 관련 참고(미채택이지만 회로 지식으로 보존): Wiegand D0/D1은 idle High, 비트마다 Low 펄스
> (폭 약 50us, 비트 간격 약 1~2ms)다. 묶으려면 active-low이므로 "둘 중 하나가 Low면 출력 Low",
> 즉 논리적으로는 AND(74HC08)가 필요하다. 다이오드 OR로 만들면 순방향 강하 때문에 Low 레벨이
> 약 0.7V까지만 떨어져 VIL 마진이 빠듯하다.

-> 최종 보드의 I/O 부족(주의 2) 대응은 별개로 유효:
  - **느린 입력 32개 / 출력 16개는 I/O 익스팬더(MCP23017 등)나 시프트 레지스터로 분리**
    (Wiegand의 50us 펄스는 I2C 익스팬더로 못 받으므로 **D0/D1에는 익스팬더를 쓰면 안 된다**)
  - 또는 **같은 STM32C562 계열에 상위 핀수 패키지가 있는지 확인**해 최종 보드에 채택

-> **개발보드 단계에서는 2모듈(리더 2채널 + 입력 6 + 출력 2)만 붙여 프로토콜을 검증**하고,
   8모듈 전체는 실제 RRU 보드에서 다룬다. 아래 "개발 단계 계획" 절 참고

### (참고) UART 안 — 채택하지 않음, 조사 기록 보존

USB로 확정했으므로 아래는 참고용 기록이다. 나중에 UART로 선회할 일이 생기면 여기서 이어가면 된다.

확인한 자료: `/home/jayden/workspace/radxa/cm3/`
(`radxa_cm3_io_board_v1360_schematic.pdf`, `radxa_cm3_v1.32a_schematic.pdf`,
`radxa_cm3_v1.3_pinout.xlsx`, `radxa_cm3_datasheet.pdf`)

**확실한 사실**

- RK3566 자체는 UART0~UART9를 가지고 있고, 모듈(CM3) 핀아웃상 대부분이 핀먹스로 노출된다
- **40핀 헤더(CON1)의 8번/10번 핀이 `UART2_TX` / `UART2_RX`** — 라즈베리파이 40핀 배치와 동일한
  위치(8=TXD, 10=RXD)이고, 각각 100R 직렬저항(R167/R168)을 거쳐 나간다.
  **IO 보드에서 UART로 명시적으로 배선된 것은 이 UART2 하나뿐이다**
- **UART1(M0)은 쓸 수 없다** — 모듈 회로도상 WiFi/BT 모듈의 HCI UART로 이미 점유
  (`UART_TXD`/`UART_RXD`/`UART_RTS_N`/`UART_CTS_N`)
- **IO 보드에 RS-232 트랜시버가 없다** — 회로도 전체에 MAX3232/SP3232 계열 부품이 없고,
  40핀 헤더는 3.3V TTL 레벨 그대로 나온다

**UART2를 쓸 때의 문제**: UART2는 Rockchip 기본 **디버그 콘솔**(`ttyS2`)이다. 그대로 RRU에 물리면
- 부팅할 때마다 U-Boot/커널 부트 메시지가 RRU로 쏟아진다 (RRU가 쓰레기 데이터를 견뎌야 함)
- 반대로 RRU가 능동 송신한 카드 이벤트가 콘솔/getty로 들어간다

-> 쓰려면 부트로더 환경변수와 커널 cmdline에서 `ttyS2` 콘솔을 꺼야 하는데,
   headless 장치에서 디버그 콘솔을 포기하는 셈이라 **6단계 개발 중에는 특히 불편하다.**

**따라서 UART로 간다면 콘솔이 아닌 별도 UART를 써야 한다.** (이 불확실성이 USB를 검토하게 된 이유)

- [ ] **실보드에서 사용 가능한 UART 오버레이 확인 (가장 확실한 방법)**
  ```bash
  ls /boot/dtbo/ | grep -i uart      # Radxa가 제공하는 UART 디바이스트리 오버레이 목록
  rsetup                             # Overlays 메뉴에서 활성화
  ls -l /dev/ttyS*                   # 활성화 후 노드 확인
  ```
  여기 나오는 목록이 "이 보드에서 실제로 지원되는 UART"의 정답이다.

**문서 간 불일치 (확인 필요)**: 핀아웃 xlsx상 `GPIO4_A4`=`UART9_TX_M2`, `GPIO4_A5`=`UART9_RX_M2`이고
IO 보드 40핀 헤더 36번/35번 핀이 이 둘이라 UART9(M2)가 후보로 보인다. 그런데 같은 xlsx의
"Function CM3 IO Board" 열은 `GPIO4_A4`를 `PI_NLED_ACTIVITY`, `GPIO4_A5`를 `BT_NDIS`로 적고 있어
서로 어긋난다 (문서 리비전 차이로 추정: 핀아웃은 모듈 v1.3, 회로도는 IO 보드 v1.36).
**실보드에서 위 오버레이 목록으로 확인 후 확정할 것.**

**결론 / 우선순위**

1. `/boot/dtbo/`에 콘솔이 아닌 UART 오버레이가 있으면 → **그것을 사용** (콘솔 보존, 부트 쓰레기 없음)
2. 없으면 → **UART2(40핀 8/10번) + 콘솔 비활성화**. 배선은 확실하지만 디버그 콘솔을 잃는다

### (보류) RS-232 레벨 변환 — UART 안으로 갈 경우에만

40핀 헤더 UART는 **3.3V TTL**이고 RS-232는 ±5~15V 반전 신호라 **직결하면 안 된다.**
ACU 쪽과 RRU 쪽 **양쪽 모두** MAX3232/SP3232 계열 트랜시버가 필요하다.

**같은 함체로 확정**되었으므로 RS-232로 갈 이유는 사라졌다. UART 안을 다시 택하더라도
트랜시버 없이 **3.3V TTL 직결**로 충분하다.

### RK3568 / CAN 경로 (이번 범위 제외)

RK3568 기반 구성에서는 하위 통신을 UART 대신 **CAN bus**로 가져갈 예정이나,
보드 미확보 상태이며 현재 범위에서는 진행하지 않는다.
다만 HAL 아래에 전송 계층(UART/CAN)을 갈아끼울 수 있는 구조로 만들어 두면
나중에 프로토콜 상위 계층은 재사용할 수 있다.

---

## RRU 보드 (Remote Reader Unit)

| 구분 | 수량 | 비고 |
|------|------|------|
| 리더 (Wiegand) | 8 | 리더 1개 = 도어 1개 기준 |
| 입력 (Input / Sensor) | 24 = 3 x 8 | 도어당 Exit, Door, Lock 3개 |
| 출력 (Output / Relay) | 8 | 도어당 1개, 채널별 NO/NC 선택 |

### 채널 구성표 (번호 체계 제안 - 확정 필요)

도어 1개당 리더 1 + 입력 3 + 출력 1이 한 묶음으로 대응된다는 전제.

| 도어 | 리더 | 입력 (Exit) | 입력 (Door) | 입력 (Lock) | 출력 (Relay) |
|:----:|:----:|:-----------:|:-----------:|:-----------:|:------------:|
| 1 | R1 | IN1  | IN2  | IN3  | OUT1 |
| 2 | R2 | IN4  | IN5  | IN6  | OUT2 |
| 3 | R3 | IN7  | IN8  | IN9  | OUT3 |
| 4 | R4 | IN10 | IN11 | IN12 | OUT4 |
| 5 | R5 | IN13 | IN14 | IN15 | OUT5 |
| 6 | R6 | IN16 | IN17 | IN18 | OUT6 |
| 7 | R7 | IN19 | IN20 | IN21 | OUT7 |
| 8 | R8 | IN22 | IN23 | IN24 | OUT8 |

### 입출력 채널 의미

- **Exit** — 퇴실/개방 버튼. 눌리면 판정 없이 해당 도어 릴레이 동작
- **Door** — 도어 접점(Door Contact). 문이 실제로 열렸는지/닫혔는지
- **Lock** — 잠금 상태(Lock State). 락이 실제로 걸렸는지
- **Relay(출력)** — 도어 릴레이. 인증 성공 시 설정된 시간(초)만큼 동작.
  채널별 **NO(Normal Open) / NC(Normal Close)** 선택 가능 -> 설정 항목으로 노출 필요

---

## 개발 단계 계획: 2모듈 선행 검증 -> 8모듈 본 제품

개발보드(NUCLEO-C562RE)에는 **8모듈 전체를 붙일 만한 I/O가 없다.**
그래서 **본 제품 보드를 설계하기 전에, 8모듈 개념 중 2모듈만 구현해 검증**하고,
검증이 끝난 뒤 8모듈 실보드를 설계한다.

### "모듈" 정의

여기서 말하는 **모듈 1개 = 도어 1개분 묶음**이다.

| 구성 | 수량 | 필요 I/O |
|------|------|----------|
| 리더 (Wiegand D0/D1) | 1 | 2 (인터럽트 입력) |
| 입력 (Exit / Door / Lock) | 3 | 3 (폴링) |
| 출력 (릴레이) | 1 | 1 |
| **합계 (모듈 1개)** | | **6 핀** |

- 본 제품: **8모듈** = 리더 8(x2선=16) + 입력 32 + 출력 16 = **64 핀** -> LQFP64로는 한계 초과 ("주의 2")
- 개발보드: **2모듈** = 리더 2(4핀) + 입력 6 + 출력 2 = **12 핀** -> 개발보드로 충분히 여유

### 진행 순서

1. **NUCLEO-C562RE 개발보드 구매** (현재 여기)
2. 보드 브링업 — 클럭, USB CDC-ACM 열거 확인(`/dev/ttyACM0`), ST-LINK VCP로 디버그 로그 분리
3. **2모듈 구성으로 배선** — 리더 2대, 입력 6, 출력 2(릴레이 모듈 또는 LED로 대체)
4. Wiegand 수신 펌웨어 — 인터럽트 직결(A안), 26/34bit 프레임 조립
5. **ACU<->RRU 프로토콜 구현·검증** (이 저장소 6단계 작업의 본체)
6. 검증 완료 후 **8모듈 실보드 설계** — 패키지 재선정, 핀 배치, I/O 익스팬더 여부 결정

### 2모듈로 검증되는 것 / 안 되는 것

**검증됨** (채널 수와 무관한 것들 — 6단계 작업의 대부분):

- USB CDC-ACM 링크, 재열거/재연결, 이벤트 버퍼링 + ACK
- 프레임 구조, CRC, 재전송, 타임아웃, 연결 끊김 감지
- Wiegand 수신 정확도, 카드 ID 형식(26/34bit) -> DB `card_id` 매핑
- **채널 번호 체계와 다채널 처리 로직** — 2채널이라도 "채널이 여러 개"라는 구조는 그대로 검증된다.
  ACU 쪽 단일 도어 전제 제거(TODO 6-1)도 2채널이면 충분히 드러난다
- 입력(Exit/Door/Lock) -> 이벤트 반영, 출력(릴레이) 제어

**검증 안 됨** (실보드에서 확인할 것):

- 16개 Wiegand 인터럽트의 **동시 부하** — 8리더에 동시에 카드를 대는 상황의 ISR 경합
- **핀 배치 제약** — 인터럽트 라인 공유를 피하는 16핀 배치 (개발보드는 2리더뿐이라 안 걸림)
- I/O 총량 부족 대응(익스팬더/상위 패키지)의 실제 동작
- 릴레이 8개 동시 동작 시 EMI / 전원 강하

> 소프트웨어(acud)는 **처음부터 8채널을 전제로 만든다.** 2모듈은 어디까지나 하드웨어 검증 범위의
> 제약이지 소프트웨어 설계의 제약이 아니다. 채널 수는 config로 두고, 개발 단계에서는 2로 쓴다.

---

## IDTi 프로토콜 매핑

기존 상위 시스템과 호환되어야 하므로, 위 구성을 IDTi 규격에 아래와 같이 대응시킨다.

| RRU 구성 | IDTi 대응 | 근거 문서 |
|----------|-----------|-----------|
| 리더 8채널 | Reader Device Type `0x8d~0x91` | `7. Device Type Table` |
| 입력 Exit | `Door Control(Exit Button) 0xae` | `7. Device Type Table` |
| 입력 Door | `Door Contact 0xac` | `7. Device Type Table` |
| 입력 Lock | `Lock State 0xad` | `7. Device Type Table` |
| 입력 32채널 전체 | Device Input(Sensor) Object `0x2C`, Index 1~254 | `9. Relay & Sensor` |
| 출력 16채널 | Device Output(Relay) Object `0x2D`, `ActiveType=1(Door Relay)` | `9. Relay & Sensor` |
| 출력 NO/NC | Sensor 쪽 `ActiveType`(0=NC, 1=NO)과 같은 개념 | `9. Relay & Sensor` |

**입력 3종이 IDTi 센서 3종과 정확히 일치**한다는 점이 중요하다
(Exit/Door/Lock = `0xae`/`0xac`/`0xad`). 별도 변환 없이 그대로 매핑된다.

채널 Index 범위가 1~254이므로 8채널/24채널은 여유 있게 들어간다.

### 프로토콜 버전

**V2 유지** (Device Status 234byte). V2는 최대 28리더를 지원하므로 8리더는 충분히 들어간다.
5단계에 적어둔 선택 근거("단일 도어/단일 리더 장치에는 V3가 과함")는 구성 변경으로 더 이상
맞지 않으니, **"8리더는 V2 범위(28) 안에 들어감"** 으로 근거를 바꿔 읽을 것.

---

## RRU 여러 대 구성 (2026-09-11 결정)

**결정**
- RRU를 **종류별로** 만든다: **모듈2**(리더 4 — 기존 SSC-R4와 같은 크기), **모듈4**(리더 8)
- **모델명(가칭)**: **`RRU-M2`**(모듈2), **`RRU-M4`**(모듈4). 정식 모델명이 정해지면 바꾼다
- ACU 1대에 **RRU 여러 대**를 붙인다. 모두 **같은 함체 안**, 연결은 **USB** 유지
- RRU는 **모델명**을 가진다 — 모델마다 모듈 수가 달라, ACU는 RRU가 알려 주는 모델명으로 리더·입출력 구성을 안다
- RRU 구분은 **RRU의 DIP 스위치 ID** (함체 안에서 한 번 설정하는 값이라, ACU 주소 설정에 스위치를 쓰지 않기로 한 것과 충돌하지 않는다)
- 요구는 16대 정도였으나 **프로토콜 모듈 14개까지로 제한한다** (2026-09-11 사용자 결정 — 아래 (가)).
  RRU 대수가 아니라 **모듈 합계**가 한계다: `RRU-M2`만이면 **7대**, `RRU-M4` 3대 + `RRU-M2` 1대면 4대

> "모듈" 용어 주의: 이 문서 앞쪽의 "8모듈"은 **리더 단위**를 뜻했다. 프로토콜의 모듈은 **리더 2개 묶음**이다.
> 여기서 모듈2 / 모듈4는 **프로토콜 모듈 수**다 (모듈2 = 리더 4, 모듈4 = 리더 8).

### 프로토콜 모듈 수 한계 — V2 모듈 14개

| 프로토콜 | 모듈 최대 | 근거 |
|----------|----------:|------|
| **V2** (지금 사용, DeviceType 33 = SSC-324) | **14** | `clsDevStatus.countDeviceModule = 14` |
| V3 | 32 | `countDeviceModuleThree = 32`, `ModuleID_Ver3.Max = 0x20` |

| 구성 | 모듈 | 리더 | V2 | V3 |
|------|-----:|-----:|:--:|:--:|
| 모듈2 × 7 | 14 | 28 | ✅ | ✅ |
| 모듈4 × 3 + 모듈2 × 1 | 14 | 28 | ✅ | ✅ |
| 모듈2 × 16 | 32 | 64 | ❌ | ✅ (정확히 한계) |
| 모듈4가 섞인 16대 | 32 초과 | — | ❌ | ❌ |

**기존 장비도 V2 한계에 맞춰져 있었다** (SSC-332/334/324 매뉴얼):
- `SSC-R4(4-Reader I/O Board) Slave Connection`, `SSC-R4 MAX 28-Door(28-Reader)`
- `SW3 : SSC-R4 Enable, RRE Setup Number 1~6 Only Used`
- 자체 리더 4 + SSC-R4 6대 × 4 = **리더 28 = 모듈 14** — V2 한계와 정확히 일치
- `SW1 : Address Select ... 0(1) ~ F(16)` + `High Address Select (17~32)` — 통신 주소 스위치(16 + 확장 16)

- [x] ~~**최대 RRU 대수 결정**~~ -> **(가) V2 유지, 모듈 14개까지** (2026-09-11 사용자 결정)
  - 기존 장비와 같은 한계(리더 28). DM·Platinum 변경 없음
  - 버린 안 (나) V3 모듈 32개 — Device Status 형식과 컨트롤러 종류가 달라져 "기존 장비 대체" 원칙과 충돌

### RRU 모델 (가칭, 2026-09-11)

| 모델명 (가칭) | 모듈 | 리더 | 입력 | 출력 | 모듈당 슬롯 14개 |
|---------------|-----:|-----:|-----:|-----:|------------------|
| **`RRU-M2`** | 2 | 4 | 16 | 8 | 리더 2 · 입력 6 · 출력 4 · 알람/화재 입력 2 |
| **`RRU-M4`** | 4 | 8 | 32 | 16 | 〃 |

- 이름 규칙: `RRU-M<모듈 수>`. 모듈 수가 다른 모델이 더 생기면 같은 규칙으로 붙인다
- 모듈당 슬롯 구성은 지금 acud가 보고하는 값(`22333333444433`)과 같다. 입력 수는 알람·화재 입력 포함
- 모델명은 RRU가 알려 주는 **문자열**로 받는다. ACU는 **모델명 → 구성표**를 코드에 가진다

### 모듈 번호 배정 — DIP ID = 시작 모듈 위치 (제안, 2026-09-11)

RRU의 DIP 스위치 값을 **그 RRU가 차지하는 첫 모듈 위치**로 쓴다.

| 설치 예 (`RRU-M4` 3대 + `RRU-M2` 1대) | DIP 값 | 차지하는 모듈 위치 |
|------|------:|------|
| `RRU-M4` 첫째 | 0 | 0 ~ 3 |
| `RRU-M4` 둘째 | 4 | 4 ~ 7 |
| `RRU-M4` 셋째 | 8 | 8 ~ 11 |
| `RRU-M2` | 12 | 12 ~ 13 |

- 모듈 위치 0~13 = Device Status `IsExistModule` bit 0~13. **4bit DIP**(0~15)로 충분하다
- **RRU 한 대가 빠져도 나머지 모듈 번호가 그대로다** — DM 포인트가 모듈 번호에 묶여 있어 중요하다.
  빠진 RRU의 모듈은 `IsExistModule`에서 bit를 내리고 USB 단절 이벤트(`0x20030102`)를 올린다
- 기존 SSC-324도 통신 주소를 스위치로 정했다(`SW1 Address Select 0(1) ~ F(16)`) — 같은 방식이다
- ACU가 **연결을 거부**하는 경우 (로그를 남기고, 해당 RRU LED로 알린다):
  - 모듈 범위가 14를 넘는다 (예: `RRU-M4`에 DIP 12 → 12~15)
  - 이미 연결된 RRU와 범위가 겹친다 (예: DIP 0 `RRU-M4`와 DIP 2 `RRU-M2`) — **먼저 붙은 쪽을 유지**
  - 모르는 모델명
- 설치자는 "앞 RRU의 DIP 값 + 모듈 수"를 다음 RRU에 넣는다. 설치 안내서에 표로 싣는다
- 대안(버림): DIP ID 순서대로 모듈을 이어 붙이기 — 한 대가 빠지거나 추가되면 뒤 번호가 전부 밀린다

### 여러 대가 동시에 통신할 때 지연 (추정, 2026-09-11)

**측정값이 아니라 추정이다.** RRU 개발보드 도착 후 실측할 것.

USB는 호스트(ACU)가 장치를 차례로 불러 받는 방식이라 RS-485처럼 **동시 송신 충돌이 없다.**
Full Speed(12Mbps) 대량 전송 용량은 초당 수백 kB, 카드 이벤트는 건당 수십 byte라 **대역폭은 병목이 아니다.**

| 구간 (카드 태그 → 릴레이) | 예상 | RRU 대수 영향 |
|---------------------------|------|---------------|
| Wiegand 수신 (26~34bit + 끝 판정) | 70~120ms | 없음 (RRU마다 따로) |
| USB로 ACU 전달 | 1~10ms | 거의 없음 |
| acud가 읽기 시작 | **지금 최대 2초** (`CARD_POLL_INTERVAL_MS 2000`) | — |
| 출입 판정 (SQLite) | 약 1ms | 동시 태그 수만큼 대기 |
| 릴레이 명령 전달 + 동작 | 10~25ms | 거의 없음 |
| **합계** | **100~160ms** (acud 이벤트 구동으로 바꾼 뒤) | |

**실제 병목**
1. **acud의 2초 주기 조회** — RRU 데이터가 들어오는 즉시 처리하도록 바꿔야 한다
2. ~~DM으로 이벤트 올리는 경로~~ — **전달 속도는 여유가 있다** (2026-09-11 사용자). 문제는 속도가 아니라
   **지금 큐가 메모리 32건이라 넘치거나 재부팅하면 이벤트가 사라진다**는 것 → 아래 "이벤트 저장" 절
3. **화재 시 ACU끼리 전파는 DM 속도** — DM이 정책 실행 전 폴링을 멈추고 1초 대기, 컨트롤러마다 순차 접속
   (접속 타임아웃 10초). ACU 한 대 안에서 여러 RRU에 해정 명령을 보내는 것은 수십 ms

### 설계 할 일

- [ ] acud를 **이벤트 구동**으로 — RRU 장치들의 fd를 select에 넣고 2초 주기 조회 제거
- [ ] **이벤트 영속 저장** — 메모리 큐 32건을 저장소로 바꾼다. 아래 "이벤트 저장" 절
- [ ] **USB 허브 구성** — CM3 IO 보드 허브 뒤 포트 4개(J18 내부 2 + 외부 Type-A 2). 최대 `RRU-M2` 7대라
      **포트 7개 이상** 필요 → 자체 IO 보드에 7포트 허브를 넣거나 허브 추가.
      **허브 하나가 재연결되면 그 뒤 RRU가 전부 함께 끊긴다** (릴레이 노이즈 재연결 위험 — 위 USB 절)
- [ ] **RRU 식별 프로토콜** — 연결 직후 RRU가 **DIP ID + 모델명 + 펌웨어 버전**을 알린다.
      ACU는 **모델명 → 모듈 수·리더·입력·출력 구성표**를 가지고 Device Status를 만든다.
      거부 조건은 위 "모듈 번호 배정" 표
- [x] ~~RRU 모델명~~ -> 가칭 `RRU-M2` / `RRU-M4` (정식 모델명 나오면 교체)
- [ ] **모듈 번호 배정 규칙 확정** — 제안: DIP ID = 시작 모듈 위치 (위 절). 사용자 확인 대기
- [ ] acud: 모듈 구성을 `config.json` 고정값(`module_count` 등)이 아니라 **연결된 RRU들로부터** 만든다

---

## 이벤트 저장 — DM 없이 독자 운영 (2026-09-11)

**요구** (사용자)
- DM과 연결이 끊겨도 NCU는 **독자적으로 출입 통제**를 계속한다 (카드 판정은 이미 ACU 로컬 DB로 한다)
- 그동안 생긴 이벤트를 **ACU가 가지고 있다가** 다시 연결되면 DM에 올린다
- 보관량은 **몇백만 건**
- DM으로 올리는 속도는 여유가 있다 — 문제는 **보관**이다

**지금 상태**: 메모리 큐 **32건**. 넘치면 오래된 것부터 버리고, 재부팅·정전이면 전부 사라진다 → **바꿔야 한다**

### 기존 장비 기준

SSC-332/334 매뉴얼 SPECIFICATION: `Event : 1,000,000`, `USER : 130,000 / 260,000`, `MEMORY : 4Giga-Byte(SD Card)`.
**기존 장비가 100만 건**이므로 몇백만 건은 기존보다 넉넉한 목표다.

DM에는 컨트롤러에 저장된 이벤트를 다루는 명령이 이미 있다 (`clsDevEvent.cs`):
**EventCountCheck**(저장 건수 4byte), **EventIndexChange**(`FromDateTime`=날짜부터 / `FromStartToEnd`=기간 지정 재전송).
SSC-324가 이 명령에 어떻게 답하는지는 DM 쪽에 확인해 같은 방식으로 구현한다.

### 1건당 크기 — 실측 (SQLite 100만 건 삽입, 2026-09-11)

| 저장 방식 | 1건 | 100만 건 |
|-----------|----:|---------:|
| 정수 필드(시각·코드·모듈·리더·문 상태·Access ID·전송 여부) | 26.9 byte | 26.9 MB |
| 위 + **시각 인덱스** (기간 재전송용) | **39.9 byte** | **39.9 MB** |
| IDTi Event Info 36byte를 BLOB 그대로 | 45.6 byte | 45.6 MB |

SQLite 파일 형식은 x86·ARM이 같아 PC 실측값이 보드에도 맞다. 아래 계산은 **여유를 두어 1건 50byte**로 잡는다.

### 보관 가능 건수

| 보관량 | 파일 크기 (50byte/건) |
|-------:|---------:|
| 100만 건 (기존 장비) | 50 MB |
| 500만 건 | 250 MB |
| **1,000만 건** | **500 MB** |
| 3,000만 건 | 1.5 GB |

- 제품 저장소는 CM3 eMMC **7.3GB**. OS A/B(각 1~1.5GB) + 설정을 빼도 데이터 영역 **3~4GB → 6,000만 건 이상** 들어간다.
  **용량은 한계가 아니다.** 한계는 정책으로 정한다
- 제안: **1,000만 건 링 보관**(500MB). 넘치면 가장 오래된 것부터 **DM에 올린 것만** 지운다

### 몇 년치인가 (추정 — 리더 28개 기준)

| 현장 | 리더 1개 하루 | 하루 | 1년 | 1,000만 건 보관 기간 |
|------|-------------:|-----:|----:|----------:|
| 한산 | 50건 | 1,400 | 51만 | 약 19년 |
| 보통 | 200건 | 5,600 | 204만 | 약 4.9년 |
| 혼잡 (출퇴근 집중) | 500건 | 14,000 | 511만 | 약 2년 |

DM이 **한 달 끊겨도** 혼잡 현장 42만 건 — 1,000만 건 보관이면 넉넉하다.

### 한 달 끊긴 뒤 다시 올리는 시간

지금 방식(응답당 1건, 3초 폴링)으로 42만 건이면 **약 15일**. 전달 속도 자체는 여유가 있다고 했지만,
**밀린 이벤트를 따라잡는 데는** 응답 하나에 여러 건을 싣는 방식이 필요할 수 있다. SSC-324가 한 응답에 몇 건을 싣는지 DM 쪽에 확인한다.

### 저장소 수명 (추정)

- 이벤트 1건 커밋 ≈ WAL 기록 8~12KB. 혼잡 현장 하루 14,000건이면 **하루 약 170MB, 1년 약 60GB** 쓴다
- 8GB eMMC는 보수적으로 잡아도 수 TB 기록을 견딘다 → **수십 년 이상**. 1초 단위로 묶어 커밋하면 더 줄어든다
- 지금 보드 eMMC(`DG4008`) 수명 표시 `life_time 0x01 0x01`(사용 0~10%), `pre_eol_info 0x01`(정상)
- ⚠ 수명 계산은 제조사 TBW 값을 확인하지 않은 추정이다. 제품 eMMC 선정 때 TBW를 확인할 것

### 설계 원칙

- [ ] **정전에 안전** — RRU에서 받은 이벤트는 **SQLite에 커밋한 뒤** "처리됨"으로 본다 (WAL 모드, `synchronous=NORMAL` 이상)
- [ ] **전송 위치 표시** — DM이 응답을 받아 간 이벤트까지 표시(`sent`). 재연결하면 그다음부터 올린다
- [ ] **카드 DB와 파일 분리** — `events.db` 따로. 이벤트가 많아져도 카드 판정 속도에 영향이 없게
- [ ] **링 삭제는 묶어서** — 한 건씩 지우지 말고 1만 건 단위로. `auto_vacuum` 대신 빈 페이지 재사용
- [ ] **DM 명령** EventCountCheck / EventIndexChange 응답 구현 — SSC-324 동작 확인 후
- [ ] 저장소가 가득 차 **아직 안 올린 이벤트까지 지워야 할 때**의 처리 — 기존 장비 동작 확인

---

## 미확정 사항 (6단계 착수 전 결정 필요)

- [ ] **STM32C562 패키지 라인업 확인** — LQFP64(`RE`)보다 핀 많은 옵션이 있는지 (최종 보드용)
- [x] ~~EXTI / 인터럽트 핀 확인~~ -> **외부 인터럽트 충분함 확인. D0/D1 16개 직결(A안) 채택** (위 "주의 3")
- [ ] **NUCLEO-C562RE 개발보드 구매** — 이후 2모듈 구성으로 검증 진행 (위 "개발 단계 계획")
- [ ] **최종 보드의 16개 Wiegand 핀 배치** — 인터럽트 라인 공유를 피하는 배치. 실보드 설계 단계 사안
- [ ] **J18(내부 USB 헤더) 결선 정의** — 2포트 중 어느 포트를 쓸지, VBUS 연결 여부, GND 처리
- [ ] **RRU 식별** — VID/PID로는 여러 대를 구분할 수 없다. **RRU의 DIP 스위치 ID**를 프로토콜로 받아 모듈 번호에
      연결한다. udev 규칙은 VID/PID로 "RRU 장치들"만 거르는 용도로 쓴다 (2026-09-11)
- [ ] **ACU-RRU 프로토콜 설계** — 프레임 구조, CRC, 릴레이 제어 명령, 센서 상태 조회/통지,
      재전송·타임아웃, RRU 연결 끊김 감지
      (장치 주소 필드 불필요 / 카드 이벤트는 RRU 능동 송신 — 위 "ACU <-> RRU 링크"에서 결정됨)
- [ ] **Wiegand 카드 ID 형식** — 26bit(facility 8 + card 16) / 34bit 중 무엇을 쓸지, 그리고 이것을
      현재 DB 스키마의 `card_id`(hex 16자) 형식에 어떻게 담을지
- [ ] **채널 번호 체계 확정** — 위 표는 제안값. RRU 펌웨어 쪽 번호와 일치시켜야 함
- [x] ~~**RRU 미연결/장애 시 ACU 동작**~~ -> **DM에 이벤트로 알린다** (2026-09-10 결정). 상세는 "제품 I/O 구성" 절
- [ ] **장치 타입 코드 확정** — RRU가 IDTi Remote Module(RIM/ROM/RRM/RXM) 중 무엇에 대응하는지,
      Device Status의 `ExistedModule`/`IOModuleStatus`로 RRU를 보고해야 하는지
      (현재 코드는 전부 0으로 채우고 있음. 미확인 문서 `8. System Device Reader Setup` 확인 대상)

---

## 제품 I/O 구성 — SSC-324와 동일한 모듈 구조 (2026-09-10 확정)

**근거 문서**
- 기존 제품 매뉴얼: `/home/jayden/workspace/idti/IDTI_Setup/SD카드/MANUAL/Manual.pdf` (SSC-332/334, SSC-324 V2.0)
- DM 쪽 규약 문서: 저장소 루트 `NCU_장치구조_프로토콜_안내.md` (서버 파서 + 현장 SSC-324 실측값 기준)

**설계 규칙 (매뉴얼)**: `1Reader per 3-Sensor(Input), 2-Relay(Output)` — 리더 1개당 입력 3 + 출력 2.

| 단자 | 이름 | 기본 기능 | 범위 |
|------|------|-----------|------|
| SEN1_1 | EXIT | 출입 버튼 | 리더별 |
| SEN1_2 | DOOR | 도어 접점 | 리더별 |
| SEN1_3 | LOCK | 잠금 접점 | 리더별 |
| CN3/CN4 | RELAY | 도어 스트라이크 (N/C·COM·N/O) | 리더별 2개 |
| **SEN0_1** | **ALARM** | 알람 센서 | **모듈(리더 2개)마다 1개** — SSC-324 실물 확인 |
| **SEN0_2** | **FIRE** | 화재 센서 | **모듈(리더 2개)마다 1개** — SSC-324 실물 확인 |

센서 기능은 사용자가 바꿀 수 있다(매뉴얼 주석). `clsDevParams.InputType`의 대응값:
`DoorControl_ExitButton=174`, `DoorContact=172`, `LockState=173`, `StandardAlarm=181`, `FireAlarm=186`/`FireDetection=187`.

**제품 구성**: 리더 8 / 입력 32 (Exit·Door·Lock 24 + **알람·화재 8** = 모듈마다 1쌍) / 출력 16. **확장보드(SSC-R4)는 쓰지 않는다.**
(SSC-324는 확장보드와 ACU 2대 마스터/슬레이브 연결을 지원하지만 우리 제품에는 해당 없음)

**프로토콜로 보고하는 배치** — 모듈 4개, 모듈마다 14슬롯을 꽉 채운다 (SSC-324 모듈과 한 글자도 같다)

```
IsExistModule  [B0][B1] = 00 0F   (모듈 1~4)
모듈 1~4 iocategory = 2 2 | 3 3 3 3 3 3 | 4 4 4 4 | 3 3
                      리더2 |   입력 6    |  출력 4  | 알람·화재 2
```

- **슬롯 13·14(알람·화재)는 모듈마다 실제로 배선된 입력이다** — 2026-09-10 SSC-324 실물 확인: 알람 2 · 화재 2.
  서버가 세는 입력 32개가 **전부 실물**이다. 처음엔 DM 규약 문서 3절대로 "보드 공통 2개를 모듈마다 복사한 것"으로 적었으나 틀렸다
- 2026-09-10 보드에서 확인: `ExistedModule=0x000f`, 모듈 1~4 모두 `22333333444433`, 합계 리더 8·입력 32·출력 16

**기판 설계 반영**
- [ ] **알람·화재 입력 8개 (모듈마다 1쌍) — RRU 쪽, 다른 입력 24개와 같은 회로로** (2026-09-10 결정).
      MCU 핀은 늘지 않는다 — I/O 익스팬더 채널이 8개 늘 뿐이다. 드는 것은 입력 보호회로·단자·기판 면적.
      (처음엔 "보드 공통 2개, ACU IO 보드에 직접"으로 정했으나 공통 입력이라는 전제가 틀려 바꿨다)
- [ ] 입력 보호·절연 (현장 배선이 길고 화재 수신반과 연결됨)
- [ ] **화재 해정 = ACU가 RRU에 명령** (확정). **RRU는 릴레이를 스스로 구동하지 않는다** (2026-09-10 사용자 확인).
      앞서 "화재 입력을 RRU에 붙이면 RRU가 직접 해정할 수 있다"고 적었으나 이 전제로 틀렸다.
      ⚠ 그래서 **USB가 끊기면 화재 시에도 해정할 수 없다** → USB 단절을 즉시 DM에 이벤트로 알린다(아래)
- [x] ~~**화재 시 전체 개방의 전달 경로**~~ -> **DM이 처리한다. ACU는 DM에만 이벤트를 올린다** (2026-09-10 결정).
      "한 ACU가 받은 화재로 모든 문을 연다"는 운영 요구이고, 연동은 DM 몫이다
  - DM에 **이미 그 기능이 있다** (`DeveiceManager/.../frmMain.cs` 정책 명령부 확인):
    ```
    GetPolicyCommandExecuteType(eventCode)
      -> devicepolicyevent_tbl 에서 수신한 이벤트 코드 조회 -> cmdtype(ForcedOpen / Normal)
      -> devicepolicycontroller_tbl 의 컨트롤러마다 ControlDoorForcedOpenModeChange 전송
    ```
    그러니 ACU가 `0x18010401` Fire Detected를 올리면, DM 정책 표에 등록된 ACU들의 문이 열린다
  - 명령 형식: `SendStatus(3) / Change(5) / Object 206(DoorForcedOpenControl)`, Data 1byte `Normal=0 / ForcedOpen=1`.
    확인은 `RequestData(6) / Read(2) / 206`
  - 참고: DM 경유이므로 **DM PC가 꺼져 있으면 다른 ACU로 전파되지 않는다** (운영상 알고 있어야 할 한계)
  - [ ] **ACU가 Object 206 강제 개방 명령을 받아 모든 도어를 여는 처리 구현** — 화재를 감지한 ACU도
        DM 정책 대상이면 이 명령으로 열린다
- [ ] **USB(ACU↔RRU) 단절 이벤트** — DM에 올린다
  - 코드 후보 `0x20030102` **Hardware_No Response** (이벤트 구조의 Module Address 필드로 모듈 번호를 실을 수 있다)
  - **`0x20010102` Comm Halted는 쓰면 안 된다** — DM이 ACU와의 TCP가 끊겼을 때 **스스로 기록하는 코드**라
    ACU↔RRU 단절과 구분이 안 된다 (DM `frmMain.cs`의 연결 오류 처리에서 확인)
  - [x] ~~복구 코드~~ -> **올리지 않는다** (2026-09-11 회신). 통신 계열에 복구 코드가 없고 SSC-324도 올리지 않는다

**관련 이벤트 코드** (`IDTI WebApp Protocol/2. IDTi Protocol Event Structure & Event Code.doc`)

| 코드 | 이름 | 쓸 곳 |
|------|------|-------|
| **`0x18010401` / `0x18010402`** | Fire Detected / Fire Restored | **화재 입력 — SSC-324 실측 확정** (용도 3=Fire). 화면 "화재 알람 발생/복구". ⚠ 주소는 반응 출력 |
| **`0x18010306` / `0x18010307`** | Intrusion Motion Detected / Restored | **알람 입력 — SSC-324 실측 확정** (용도 0·2 모두 같음). 화면 "모션 알람 발생/복구" |
| `0x18010119` / `0x1801011A` | Force Open Mode / Release Force Open Mode | 전체 개방 적용·해제 |
| **`0x20030102`** | H/W No Response | **USB 단절 — 확정** (화면 "응답없음", 표시번호 107). Platinum 코드표에 있음 |
| `0x2003010B` | H/W Network(Chip) Error | 칩 자체 오류를 구분할 수 있을 때만 (화면 "네트워크(칩) 오류") |
| `0x20010102` | Disconnected | **DM이 ACU TCP 단절에 스스로 기록 — USB용으로 쓰지 말 것.** (`0x20010101` Comm Started는 Platinum 코드표에 없음) |

- **2026-09-11 Platinum 회신 반영** (`NCU_이벤트코드_질의_회신.md`) — 판단 기준은 **기존 장비 대체**:
  ① SSC-324가 실제로 올리는 값 ② 없으면 기존 어휘(987개, `event_codes_all.csv`) ③ 새 코드·문구는 만들지 않는다
  - **알람**: 현장 SSC-324(ACU04) 슬롯13이 실제로 `0x18010306/07`을 올렸다 → 그대로 쓴다.
    화면이 "모션 알람"이어도 현장이 이미 보는 문구라 바꾸지 않는다. 단 측정 당시 그 입력 용도가 `0=적용 안함`이라,
    용도 `2=Alarm`에서도 같은 코드임을 DM 쪽이 실측으로 **확정**했다 (2026-09-11 14:45).
  - **입력 용도가 코드를 정한다** (DM 실측): 용도 `0`·`2` → `18010306/07` 모션 알람, 용도 `3=Fire` → `18010401/02` 화재 알람.
    ⇒ 슬롯 번호로 하드코딩하지 말고 **"입력 용도 → 이벤트 코드" 규칙**으로 구현한다
  - ⚠ **화재 이벤트 주소 규칙이 알람과 다르다** (2026-09-11 DM 실측, 표본 2개 — 재측정 후 확정 예정):
    알람은 **입력 슬롯 주소** `(모듈, 13)`로 올라오지만, 화재는 **그 입력에 연동된 반응 출력(문) 주소**로 올라온다.
    ACU03 실측: 모듈1 슬롯14(반응 출력 (1,2)) → 이벤트 (1,2), 모듈2 슬롯14(반응 출력 (2,1)) → 이벤트 (2,1).
    ⇒ ACU는 DM이 내려 주는 **입력 설정(용도·반응 출력)을 저장**해 두어야 이벤트 코드와 주소를 만들 수 있다
  - DM 규약 문서의 "알람·화재 입력은 보드 공통 2개의 사본" 서술은 DM 쪽이 **정정**했다 — 모듈마다 실물 (사용자 확인과 일치)
  - **USB 단절**: `0x20030102`. `0x2003xxxx` H/W Packet은 컨트롤러↔하위 하드웨어 통신 실패 자리다
  - **USB 복구: 올리지 않는다.** 통신 계열(`20xxxxxx`)에는 복구 코드가 하나도 없고 SSC-324도 올리지 않는다.
    DM은 폴링으로 현재 상태를 안다
  - Platinum의 코드↔문구 연결은 4단 계층이다: `eventtype_tbl`→`eventobject_tbl`→`eventcode_tbl`→**`eventerrorcode_tbl`**(8자리 결정)
  - DM 정책 엔진은 이벤트 코드를 `devicepolicyevent_tbl.eventcode`와 **문자열 정확 일치**로 찾는다.
    지금 현장의 이 표는 **비어 있다**(화재→전체개방 정책 미설정)
- 알람 `0x18010306/07` 확인 내용 (2026-09-10)
  - 코드표 두 판본(WebApp / SDK) 모두 `0x18010306` = 402719494 Intrusion Motion Detected,
    `0x18010307` = 402719495 Intrusion Motion Restored
  - **DM** `clsEventController.cs`: 분류 `1801` + 오브젝트 `03`(Alarm Ex Sensor) + 결과 `06`/`07`로 해석 표에 등록돼 있다.
    DM 화면에는 "System > Alarm Ex Sensor > Intrusion Motion Detected"로 뜨고, 정책 표의 이벤트로도 고를 수 있다
  - **Platinum** 표시 enum: `MotionDetected = 69` / `MotionRestored = 70` (화재 71/72 바로 앞).
    화면 문구(`resEventCode.ko-KR.resx`): **"모션 알람 발생" / "모션 알람 복구"**
    (영문 "Motion Detected" / "Motion Restored"). 참고로 화재는 "화재 알람 발생"
    → "모션 알람"으로 보여 한때 재검토했으나, **SSC-324 실측값이라 그대로 간다** (2026-09-11 회신)
  - ⚠ Platinum은 8자리 코드를 **DB `eventcode_tbl`의 `StringCode` 컬럼**으로 표시 문구에 연결한다.
    `18010306 -> 69` 연결은 소스로 확정할 수 없어 **DB에서 확인**해야 한다(순서상 연결돼 있을 가능성이 높다)

---

## 커미셔닝 / 주소 설정 설계 (2026-09-09 방향 확정)

**배경**: 기존 장비는 **LCD+키패드**로 현장에서, 그리고 **IntelliScan Device Manager의 search**로
원격에서 네트워크 설정을 바꿀 수 있었다. 새 ACU는 headless라 두 수단이 모두 없어진다.
설치 환경은 외부와 끊긴 로컬 네트워크(`192.168.0.x`)다.

**webui만으로는 부족하다.** webui에 접속하려면 이미 IP를 알아야 하는데, 고쳐야 하는 대상이 바로 그
IP다(닭-달걀). 그래서 **접속 불가 상태에서도 통하는 경로**가 반드시 하나 이상 있어야 한다.

> **2026-09-09 추가 결정**: OS는 **자체 빌드 / 메인라인 커널 6.x / A/B 파티션 업데이트**로 정했다.
> 자체 보드는 커스텀 디바이스트리가 필요하고, 그 브링업에는 **UART 콘솔이 반드시 있어야 한다.**
> 상세는 [TODO.md](TODO.md) "선행 결정: 최종 OS".

### 채택한 방향 (로터리/DIP 스위치는 제외)

로터리/DIP 주소 스위치는 **채택하지 않는다** (사용자 결정). 대신 아래 네 가지로 그 역할을 나눈다.
스위치가 주던 "보드를 교체해도 주소가 유지된다"는 이점은 **UDP 탐색 + PC의 설정 백업/복원**으로 대신한다.

| # | 수단 | 무엇을 대체하나 | BOM | 기판 반영 필요 |
|---|------|-----------------|-----|----------------|
| 1 | **UDP 브로드캐스트 탐색/설정** | Device Manager search, 닭-달걀 해소, 대량 커미셔닝 | 0 | 불필요 |
| 2 | **USB 커미셔닝 (+ 핸드폰)** | LCD+키패드의 "현장에서 도구 없이" | 0 | **커넥터 노출** |
| 3 | **리커버리 버튼 + 상태 LED** | 공장초기화, 상태 확인 | 낮음 | **필요** |
| 4 | **webui** | 나머지 모든 설정 | 0 | 불필요 |

### 1. UDP 브로드캐스트 탐색/설정 — 최우선

기존 `islnetmodule` 프로토콜(UDP 1460/5001, `FIND`/`IMIN`/`SETT`)을 그대로 구현한다.
프레임 구조는 [README.md](README.md)의 "netmodule UDP 탐색/설정 프로토콜"에 정리해 뒀다.

- **L2 브로드캐스트라 장치 IP를 몰라도, 대역이 달라도 찾힌다**
- **기존 Device Manager가 PC 쪽 변경 없이 우리 ACU를 발견·설정할 수 있다** — 설치기사가 쓰던 도구와
  손에 익은 절차가 그대로 유지된다. 이것이 이 방식의 가장 큰 값어치다
- 보드 교체 시: 새 장치를 탐색 -> 저장해 둔 설정을 밀어 넣으면 이전 주소를 회복한다

### 2. USB 커미셔닝 + 핸드폰

CM3 IO 보드의 **USB OTG0(micro USB)** 를 쓴다. RRU가 쓸 USB 경로(J18 내부 헤더)와 **별개 포트**라
충돌하지 않는다. OTG0은 maskrom 플래싱에 쓰이므로 device 모드가 이미 지원된다.

**핵심 제안: USB 네트워크 가젯(`g_ether`, CDC-ECM/RNDIS)으로 붙이면 전용 앱이 필요 없다.**
핸드폰이나 노트북을 꽂으면 USB로 고정 링크(예: `192.168.7.1`)가 생기고, **브라우저로 우리 webui에
바로 접속**된다. 네트워크 설정이 어떻게 망가져 있든 통한다.

- 앱을 만들지 않아도 되므로 앱 배포/서명/버전관리 부담이 사라진다
- 안드로이드는 USB 이더넷(RNDIS/ECM)을 대체로 지원하고, USB-C 아이폰(15 이후)도 지원한다
- **앱이 필요하다면** 이 위에 얹는 얇은 껍데기로 충분하다 (WebView + 장치 자동 탐색).
  앱을 먼저 만들 이유는 없다
- **보안**: USB 가젯이 열려 있으면 물리 접근자가 네트워크에 붙을 수 있다. 함체 안에 있으므로
  "물리 접근 = 이미 진 게임"이라 보는 것이 타당하지만, webui 로그인은 여전히 요구해야 한다.
  더 조이려면 **리커버리 버튼을 누른 동안에만 가젯을 올리는** 방식도 가능하다

**기판에 필요한 것**: OTG0 커넥터를 함체 외부(또는 뚜껑 열면 닿는 위치)로 뺄지 결정.
이것을 안 빼면 이 경로 자체가 없어진다.

### 3. 리커버리 버튼 + 상태 LED — 기판 필수

- **버튼 1개(GPIO 1핀)**: 부팅 시 길게 누르면 네트워크 설정을 공장 기본값으로 복구.
  가장 값싼 벽돌 보험이다. 소프트웨어 롤백(이미 구현됨)이 못 잡는 경우까지 잡는다
- **LED 최소 3개**: 전원 / 링크·통신 / 에러. "이 장치 살아 있나?"를 노트북 없이 답한다
- **(선택) I2C OLED 128x64** — 3천원 수준. IP·링크·RRU 상태·에러를 직접 표시한다.
  기존 LCD를 그리워할 현장이 있다면 여기가 답이다. 최소한 **I2C 핀과 풋프린트는 남겨 둘 것**

### 4. 그 밖에 지금 정해야 할 것

- **`192.168.0.x`를 코드에 박지 말 것** — 지금 현장이 전부 이 대역이라도 언젠가 `10.x`나
  `192.168.1.x` 고객이 나온다. 프리픽스는 설정값으로 두고 기본값만 `192.168.0`으로 한다.
  지금 정하면 공짜, 나중에 고치면 아프다
- **공장 기본 IP** — 신품 여러 대를 동시에 물리면 전부 같은 주소가 된다.
  UDP 탐색은 MAC 기준이라 그래도 찾히지만, **부팅 시 `arping -D`로 충돌을 감지해
  IP를 올리지 말고 LED로 알리게** 한다 (충돌 검사 코드는 `deploy/acu-netcfg`에 이미 있다)
- **설정 백업/복원** — 보드 교체 시 이전 주소·설정을 되살리는 유일한 수단이다.
  PC(Device Manager) 쪽에 보관되는 것이 자연스럽다

### 기판 설계 체크리스트 (미실장이라도 풋프린트는 남길 것)

> 소프트웨어는 나중에 얼마든지 추가되지만, **기판에 없는 부품은 나중에 못 넣는다.**

- [ ] **⚠ I2C 풀업 저항 (SCL/SDA)** — **2026-09-10 확인: CM3 IO 보드의 i2c-2에 풀업이 없다.**
      I2C는 오픈드레인이라 **풀업이 없으면 버스가 성립하지 않는다.** 4.7k(또는 2.2k~10k)를
      SCL/SDA 각각에서 3.3V로 올릴 것
  - 증상: `i2cdetect -y 2`가 **전체 `--`** (0x51뿐 아니라 어떤 주소도 응답 없음).
    같은 방법으로 i2c-6은 0x30이 정상 응답하므로 **스캔 기능 자체는 멀쩡하다**
  - **이것 하나가 RTC·OLED·GPIO 확장칩을 전부 막는다.** 아래 항목들이 모두 이 버스를 쓴다
- [ ] **RTC 칩 + 백업 배터리** — 보드에 **HYM8563TS(U24)** 가 이미 있고 배터리(U23)도 넣었으며
      전원 제거 상태에서 **3.2V 공급까지 확인**했다. 그런데 **위 풀업 문제로 칩이 I2C에 응답하지 않아
      커널이 배터리 없는 `rk808-rtc`로 폴백한다** (`rtc-hym8563 2-0051: could not init device, -6`).
      결과적으로 전원이 끊기면 시계가 **2017-08-04**로 돌아간다
  - **왜 중요한가**: IDTi 이벤트는 BCD 시각을 싣는다. **설치 현장은 외부와 끊긴 로컬망이라
    NTP 서버가 없을 수 있고**, 그러면 시계가 영구히 틀린다. 출입 이벤트가 증거로서 무의미해진다
  - 자체 보드에서는 **RTC 칩 + 배터리 + 풀업**을 한 묶음으로 볼 것
- [ ] **UART 콘솔 헤더** — **OS를 자체 빌드하기로 했으므로 필수다.** 자체 커널/DT를 올리다
      부팅이 안 되면 시리얼 콘솔 말고는 볼 방법이 없다. 네트워크가 뜨기 전에 죽으면 SSH도 소용없다.
      브링업의 필수품이고 원가는 거의 0이다
- [ ] **maskrom 버튼 + USB-C** — eMMC에 이미지를 굽고, 망가졌을 때 복구하는 유일한 경로
- [ ] **USB OTG0 커넥터를 외부(또는 뚜껑 안쪽)로 노출** — USB 커미셔닝 경로의 전제
      (maskrom용 USB-C와 겸용 가능한지 검토)
- [ ] **리커버리 버튼 1개** (GPIO 1핀, 풀업) — 네트워크 설정 공장초기화.
      A/B 파티션에서는 **데이터 파티션만 비우는 것**으로 정의하면 깔끔하다
- [ ] **상태 LED 3개** (전원 / 링크 / 에러)
- [ ] **I2C 헤더 + OLED 풋프린트** (선택이지만 남겨 둘 것)
      — **풀업 없이는 동작하지 않는다.** 위 I2C 풀업 항목과 한 묶음이다
- [ ] 이더넷 전용 확정에 따른 **WiFi/BT 안테나 미실장 여부** 결정
      (CM3 모듈에 하드웨어는 있다. 제품에서는 wlan0을 끄기로 했으므로 안테나를 안 달면 된다)

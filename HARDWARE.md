# 하드웨어 구성

이 프로젝트의 물리적 장치 구성 기록. 소프트웨어 진행 상황은 [README.md](README.md),
남은 작업은 [TODO.md](TODO.md) 참고.

- 최종 갱신: 2026-09-07
- 상태: **RRU 보드는 설계 단계** (실물 확보 전). 아래 채널 번호 체계는 확정 전 제안값
- 연결 방식: **ACU 1 : RRU 1 (point-to-point)**, 물리 계층은 **USB 검토 중** (기존 RS-232 안 대체)
- 설치 형태: ACU와 RRU는 **같은 함체 안**에 들어간다

---

## 전체 구성

```
  [상위 시스템 / 웹앱]
          |
          |  Ethernet - TCP / IDTi 프로토콜 V2      (5단계: 구현 완료)
          |
  +-------------------------------------------+
  |  ACU  (Access Control Unit)               |
  |  RK3566 / Radxa CM3 IO Board              |   <- 이 저장소의 acud 데몬
  |  임베디드 리눅스, headless                 |
  +-------------------------------------------+
          |
          |  USB(유력) 또는 UART - 1:1, ACU<->RRU 프로토콜  (6단계: 설계·구현 대상)
          |
  +-------------------------------------------+
  |  RRU  (Remote Reader Unit)                |
  |  리더/센서/릴레이 물리 I/O 담당             |
  +-------------------------------------------+
     |        |         |
     |        |         +-- 출력 8채널  (릴레이, 채널별 NO/NC)
     |        +------------ 입력 24채널 (Exit / Door / Lock) x 8
     +--------------------- 리더 8채널  (Wiegand)
```

**핵심**: 리더기·센서·릴레이는 ACU(RK3566)에 **직접 연결되지 않는다.**
Wiegand 수신과 GPIO 제어는 전부 RRU가 담당하고, ACU는 UART로 RRU와만 통신한다.
즉 ACU 쪽 6단계 작업은 "RK3566에서 Wiegand를 직접 받는 일"이 아니라
**"ACU-RRU 간 UART 프로토콜을 설계하고 구현하는 일"** 이다.

---

## ACU 보드

| 항목 | 내용 |
|------|------|
| SoC | RK3566 |
| 보드 | Radxa CM3 IO Board (보유 중) |
| OS | 임베디드 리눅스 (headless CLI 데몬) |
| 상위 통신 | Ethernet / TCP, IDTi 프로토콜 V2 |
| 하위 통신 | USB(유력) 또는 UART -> RRU 1대 (point-to-point) |
| 역할 | 출입 판정, DB(SQLite3), 설정 관리, 상위 시스템 연동, 이벤트 로그 |

### ACU <-> RRU 링크

| 항목 | 값 | 비고 |
|------|-----|------|
| 토폴로지 | **1 : 1 (point-to-point)** | ACU 1대에 RRU **1대만** |
| 설치 | **같은 함체 내부** | 배선 길이 문제 없음 |
| 물리 계층 | **USB 유력** (아래 참고) | RS-232 안은 보류 |
| 논리 계층 | 시리얼 바이트 스트림 | 물리 계층과 무관하게 동일 |
| baud / 프레이밍 | **115200 8N1** | USB CDC면 형식적 값이 되지만 그대로 유지 |

**이 결정이 프로토콜 설계에 주는 영향** — 물리 계층이 UART든 USB든 아래는 그대로 유지된다:

1. **프레임에 장치 주소 필드가 필요 없다.** 1:1 연결이라 "누구에게 보내는 명령인지"를 실을 이유가 없다.
   5단계에서 TCP가 1:1이라 IDTi의 RS-485 다중 장치 주소 지정을 단순화한 것과 같은 논리.
2. **RRU가 카드 이벤트를 능동적으로 먼저 보낸다.** UART(full duplex)든 USB CDC든 양방향 동시 통신이
   가능하므로 마스터-슬레이브 폴링에 묶이지 않는다. 카드 태그 즉시 ACU로 밀어 올린다.

### 물리 계층: USB 검토 (현재 유력안)

UART 경로가 핀먹스/콘솔 충돌/문서 불일치로 확실하지 않아 USB를 대안으로 검토했다.

**IO 보드 회로도 확인 결과 (USB)**

- USB 2.0 HUB를 통해 4포트: `USB2.0_1` / `USB2.0_2` (외부 Type-A 소켓),
  **`USB2.0_3_CON` / `USB2.0_4_CON` (내부 커넥터)**
- **`J18` = `DIP_10PIN-2_54MM`** — 2.54mm 10핀 헤더에 USB 2포트(`USB3_DP/DM`, `USB4_DP/DM`)가 나온다.
  PC 메인보드 내부 USB 헤더와 같은 형태로, **같은 함체 안 보드끼리 연결하기에 딱 맞다**
  (외부 Type-A 케이블 불필요)
- 그 외 USB 3.0 HOST1(Type-A), USB OTG0(micro USB)도 있음

**USB의 장점 (우리 상황 기준)**

- **UART에서 막혔던 문제가 전부 사라진다** — 핀먹스 오버레이 확인 불필요, 디버그 콘솔(ttyS2) 충돌 없음,
  문서 리비전 불일치 무관, RS-232 트랜시버(MAX3232) 양단 불필요
- 함체 내부 10핀 헤더로 바로 연결 가능
- **소프트웨어 영향이 사실상 없다** — ACU 입장에서는 여전히 `/dev/tty*`를 열고 읽고 쓰는 시리얼 포트다.
  프레임 구조/CRC/이벤트 통지 방식 등 프로토콜 설계는 그대로 재사용된다
  -> **되돌리기 쉬운 결정**. 나중에 UART로 바꿔도 `rru.c`의 포트 여는 부분만 손대면 된다

**USB의 단점 / 주의할 점**

- **링크가 끊겼다 다시 붙을 수 있다.** UART는 선만 붙어 있으면 계속 살아 있지만, USB는 재열거
  (re-enumeration)가 일어나면 1~2초 링크가 사라진다. **출입 제어 장치는 릴레이/솔레노이드 락 같은
  유도성 부하를 스위칭하므로 EMI로 인한 USB 재열거 위험이 실재한다**
  -> 완화: 릴레이 코일에 플라이백 다이오드/스너버, 접지 설계, 짧은 배선, USB 라인과 릴레이 배선 분리
  -> **프로토콜에 반드시 반영**: RRU가 이벤트를 자체 버퍼에 쌓고 ACU가 ACK할 때까지 유지.
     링크가 잠깐 끊겨도 카드 태그가 유실되지 않아야 한다 (어차피 있어야 할 설계)
- **디바이스 노드 이름이 고정이 아니다** — `/dev/ttyUSB0`/`/dev/ttyACM0` 번호가 바뀔 수 있다
  -> **udev 규칙으로 고정 심볼릭 링크 필수** (VID/PID 또는 시리얼 번호 기준, 예: `/dev/rru`)
- **부팅 시 열거 타이밍** — acud 시작 시점에 아직 장치가 안 잡혀 있을 수 있다
  -> 없으면 재시도하는 로직 필요 (net_init 실패해도 계속 도는 기존 fail-safe 원칙과 동일)
- **USB로 RRU에 전원 공급은 불가** — USB 2.0은 500mA인데 리더 8개 + 릴레이 8개는 훨씬 넘는다.
  RRU는 자체 전원을 써야 하고, USB는 신호선(D+/D-/GND)만 쓴다. **VBUS 연결 여부와 GND 처리 주의**

**RRU 쪽 USB 구현 방식 (선택 필요)**

- [ ] RRU MCU에 **네이티브 USB가 있으면 CDC-ACM**으로 구현 -> `/dev/ttyACM0`. 부품 추가 없음, 가장 깔끔
- [ ] 없으면 **USB-시리얼 브릿지 IC** 추가 (CP2102 / FT232 계열) -> `/dev/ttyUSB0`.
      값싼 CH340 클론은 드라이버 이슈가 있어 권하지 않음

### (대안) UART 포트 선정 — 회로도 확인 결과

확인한 자료: `/home/jayden/workspace/radxa/cm3/`
(`radxa_cm3_io_board_v1360_schematic.pdf`, `radxa_cm3_v1.32a_schematic.pdf`,
`radxa_cm3_v1.3_pinout.xlsx`, `radxa_cm3_datasheet.pdf`)

**확실한 사실**

- RK3566 자체는 UART0~UART9를 가지고 있고, 모듈(CM3) 핀아웃상 대부분이 핀먹스로 노출된다
- **40핀 헤더(CON1)의 8번/10번 핀이 `UART2_TX` / `UART2_RX`** — 라즈베리파이 40핀 배치와 동일한
  위치(8=TXD, 10=RXD)이고, 각각 100R 직렬저항(R167/R168)을 거쳐 나간다.
  **IO 보드에서 UART로 명시적으로 배선된 것은 이 UART2 하나뿐이다**
- **UART1(M0)은 쓸 수 없다** — 모듈 회로도상 WiFi/BT 모듈의 HCI UART로 이미 점유
  (`UART_TXD`/`UART_RXD`/`UART_RTS_N`/`UART_CTS_N`)
- **IO 보드에 RS-232 트랜시버가 없다** — 회로도 전체에 MAX3232/SP3232 계열 부품이 없고,
  40핀 헤더는 3.3V TTL 레벨 그대로 나온다

**UART2를 쓸 때의 문제**: UART2는 Rockchip 기본 **디버그 콘솔**(`ttyS2`)이다. 그대로 RRU에 물리면
- 부팅할 때마다 U-Boot/커널 부트 메시지가 RRU로 쏟아진다 (RRU가 쓰레기 데이터를 견뎌야 함)
- 반대로 RRU가 능동 송신한 카드 이벤트가 콘솔/getty로 들어간다

-> 쓰려면 부트로더 환경변수와 커널 cmdline에서 `ttyS2` 콘솔을 꺼야 하는데,
   headless 장치에서 디버그 콘솔을 포기하는 셈이라 **6단계 개발 중에는 특히 불편하다.**

**따라서 UART로 간다면 콘솔이 아닌 별도 UART를 써야 한다.** (이 불확실성이 USB를 검토하게 된 이유)

- [ ] **실보드에서 사용 가능한 UART 오버레이 확인 (가장 확실한 방법)**
  ```bash
  ls /boot/dtbo/ | grep -i uart      # Radxa가 제공하는 UART 디바이스트리 오버레이 목록
  rsetup                             # Overlays 메뉴에서 활성화
  ls -l /dev/ttyS*                   # 활성화 후 노드 확인
  ```
  여기 나오는 목록이 "이 보드에서 실제로 지원되는 UART"의 정답이다.

**문서 간 불일치 (확인 필요)**: 핀아웃 xlsx상 `GPIO4_A4`=`UART9_TX_M2`, `GPIO4_A5`=`UART9_RX_M2`이고
IO 보드 40핀 헤더 36번/35번 핀이 이 둘이라 UART9(M2)가 후보로 보인다. 그런데 같은 xlsx의
"Function CM3 IO Board" 열은 `GPIO4_A4`를 `PI_NLED_ACTIVITY`, `GPIO4_A5`를 `BT_NDIS`로 적고 있어
서로 어긋난다 (문서 리비전 차이로 추정: 핀아웃은 모듈 v1.3, 회로도는 IO 보드 v1.36).
**실보드에서 위 오버레이 목록으로 확인 후 확정할 것.**

**결론 / 우선순위**

1. `/boot/dtbo/`에 콘솔이 아닌 UART 오버레이가 있으면 → **그것을 사용** (콘솔 보존, 부트 쓰레기 없음)
2. 없으면 → **UART2(40핀 8/10번) + 콘솔 비활성화**. 배선은 확실하지만 디버그 콘솔을 잃는다

### (보류) RS-232 레벨 변환 — UART 안으로 갈 경우에만

40핀 헤더 UART는 **3.3V TTL**이고 RS-232는 ±5~15V 반전 신호라 **직결하면 안 된다.**
ACU 쪽과 RRU 쪽 **양쪽 모두** MAX3232/SP3232 계열 트랜시버가 필요하다.

**같은 함체로 확정**되었으므로 RS-232로 갈 이유는 사라졌다. UART 안을 다시 택하더라도
트랜시버 없이 **3.3V TTL 직결**로 충분하다.

### RK3568 / CAN 경로 (이번 범위 제외)

RK3568 기반 구성에서는 하위 통신을 UART 대신 **CAN bus**로 가져갈 예정이나,
보드 미확보 상태이며 현재 범위에서는 진행하지 않는다.
다만 HAL 아래에 전송 계층(UART/CAN)을 갈아끼울 수 있는 구조로 만들어 두면
나중에 프로토콜 상위 계층은 재사용할 수 있다.

---

## RRU 보드 (Remote Reader Unit)

| 구분 | 수량 | 비고 |
|------|------|------|
| 리더 (Wiegand) | 8 | 리더 1개 = 도어 1개 기준 |
| 입력 (Input / Sensor) | 24 = 3 x 8 | 도어당 Exit, Door, Lock 3개 |
| 출력 (Output / Relay) | 8 | 도어당 1개, 채널별 NO/NC 선택 |

### 채널 구성표 (번호 체계 제안 - 확정 필요)

도어 1개당 리더 1 + 입력 3 + 출력 1이 한 묶음으로 대응된다는 전제.

| 도어 | 리더 | 입력 (Exit) | 입력 (Door) | 입력 (Lock) | 출력 (Relay) |
|:----:|:----:|:-----------:|:-----------:|:-----------:|:------------:|
| 1 | R1 | IN1  | IN2  | IN3  | OUT1 |
| 2 | R2 | IN4  | IN5  | IN6  | OUT2 |
| 3 | R3 | IN7  | IN8  | IN9  | OUT3 |
| 4 | R4 | IN10 | IN11 | IN12 | OUT4 |
| 5 | R5 | IN13 | IN14 | IN15 | OUT5 |
| 6 | R6 | IN16 | IN17 | IN18 | OUT6 |
| 7 | R7 | IN19 | IN20 | IN21 | OUT7 |
| 8 | R8 | IN22 | IN23 | IN24 | OUT8 |

### 입출력 채널 의미

- **Exit** — 퇴실/개방 버튼. 눌리면 판정 없이 해당 도어 릴레이 동작
- **Door** — 도어 접점(Door Contact). 문이 실제로 열렸는지/닫혔는지
- **Lock** — 잠금 상태(Lock State). 락이 실제로 걸렸는지
- **Relay(출력)** — 도어 릴레이. 인증 성공 시 설정된 시간(초)만큼 동작.
  채널별 **NO(Normal Open) / NC(Normal Close)** 선택 가능 -> 설정 항목으로 노출 필요

---

## IDTi 프로토콜 매핑

기존 상위 시스템과 호환되어야 하므로, 위 구성을 IDTi 규격에 아래와 같이 대응시킨다.

| RRU 구성 | IDTi 대응 | 근거 문서 |
|----------|-----------|-----------|
| 리더 8채널 | Reader Device Type `0x8d~0x91` | `7. Device Type Table` |
| 입력 Exit | `Door Control(Exit Button) 0xae` | `7. Device Type Table` |
| 입력 Door | `Door Contact 0xac` | `7. Device Type Table` |
| 입력 Lock | `Lock State 0xad` | `7. Device Type Table` |
| 입력 24채널 전체 | Device Input(Sensor) Object `0x2C`, Index 1~254 | `9. Relay & Sensor` |
| 출력 8채널 | Device Output(Relay) Object `0x2D`, `ActiveType=1(Door Relay)` | `9. Relay & Sensor` |
| 출력 NO/NC | Sensor 쪽 `ActiveType`(0=NC, 1=NO)과 같은 개념 | `9. Relay & Sensor` |

**입력 3종이 IDTi 센서 3종과 정확히 일치**한다는 점이 중요하다
(Exit/Door/Lock = `0xae`/`0xac`/`0xad`). 별도 변환 없이 그대로 매핑된다.

채널 Index 범위가 1~254이므로 8채널/24채널은 여유 있게 들어간다.

### 프로토콜 버전

**V2 유지** (Device Status 234byte). V2는 최대 28리더를 지원하므로 8리더는 충분히 들어간다.
5단계에 적어둔 선택 근거("단일 도어/단일 리더 장치에는 V3가 과함")는 구성 변경으로 더 이상
맞지 않으니, **"8리더는 V2 범위(28) 안에 들어감"** 으로 근거를 바꿔 읽을 것.

---

## 미확정 사항 (6단계 착수 전 결정 필요)

- [ ] **ACU-RRU 배선 거리 확인** — RS-232 실용 한계(약 15m) 안에 들어가는지
- [ ] **RK3566에서 쓸 UART 포트 확정** — 위 "UART 포트 선정" 참고 (실보드 오버레이 확인 필요)
- [ ] **RS-232 트랜시버 vs TTL 직결** 결정 — 위 "RS-232 레벨 변환" 참고
- [ ] 흐름제어(RTS/CTS) 사용 여부
- [ ] **ACU-RRU 프로토콜 설계** — 프레임 구조, CRC, 릴레이 제어 명령, 센서 상태 조회/통지,
      재전송·타임아웃, RRU 연결 끊김 감지
      (장치 주소 필드 불필요 / 카드 이벤트는 RRU 능동 송신 — 위 "ACU <-> RRU 링크"에서 결정됨)
- [ ] **Wiegand 카드 ID 형식** — 26bit(facility 8 + card 16) / 34bit 중 무엇을 쓸지, 그리고 이것을
      현재 DB 스키마의 `card_id`(hex 16자) 형식에 어떻게 담을지
- [ ] **채널 번호 체계 확정** — 위 표는 제안값. RRU 펌웨어 쪽 번호와 일치시켜야 함
- [ ] **RRU 미연결/장애 시 ACU 동작** — 로그만 남기고 계속 동작할지, 상위 시스템에 장애 이벤트를 올릴지
- [ ] **장치 타입 코드 확정** — RRU가 IDTi Remote Module(RIM/ROM/RRM/RXM) 중 무엇에 대응하는지,
      Device Status의 `ExistedModule`/`IOModuleStatus`로 RRU를 보고해야 하는지
      (현재 코드는 전부 0으로 채우고 있음. 미확인 문서 `8. System Device Reader Setup` 확인 대상)

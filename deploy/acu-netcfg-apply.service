[Unit]
Description=Apply network config requested by ACU UDP discovery (SETT)
After=NetworkManager.service

[Service]
Type=oneshot
# 요청 파일은 acu-netcfg가 읽은 뒤 지운다 (실패해도 지운다 - 같은 요청이 무한 재시도되면 안 된다)
ExecStart=/usr/local/sbin/acu-netcfg apply-request /run/acud/netcfg-request

# NCU 질의 — RRU 여러 대 구성 · 이벤트 보관 · 입력 설정

> 보내는 쪽: NCU(신규 ACU) 펌웨어 개발
> 작성: 2026-09-11
> 요청: 아래 **질의 1~7**에 대한 답. 맨 끝 **회신 양식**에 채워 주시면 됩니다.
> 앞선 회신(`NCU_이벤트코드_질의_회신.md`)과 실측 감사합니다. 알람·화재 코드는 회신대로 확정했습니다.

---

## 0. 배경 — 그사이 NCU 쪽에서 바뀐 것

판단 기준은 그대로 **기존 IDTi 장비(SSC-324) 대체**입니다.

### 0-1. RRU(I/O 보드)를 여러 대 붙입니다

| 항목 | 결정 |
|---|---|
| 연결 | NCU 1대 ↔ RRU **여러 대**, 같은 함체 안, USB |
| RRU 종류 (가칭) | **`RRU-M2`** = 모듈 2 (리더 4 · 입력 16 · 출력 8) — **SSC-324 본체와 같은 모양**<br>`RRU-M4` = 모듈 4 (리더 8 · 입력 32 · 출력 16) |
| 개발 우선 대상 | **`RRU-M2`** |
| 모듈 수 한계 | **모듈 합계 14개** (Protocol V2 한계, 기존 SSC-R4 최대 28-Reader와 같음). V3는 쓰지 않습니다 |
| 모듈 번호 | RRU의 DIP 스위치에 **RRU 번호 1~7**. **번호 하나 = 모듈 2칸** |

```
RRU 번호 1 → 모듈 1~2      RRU 번호 2 → 모듈 3~4   …   RRU 번호 7 → 모듈 13~14
RRU-M4 는 번호 두 개를 씁니다 (번호 1이면 모듈 1~4, 다음 RRU는 번호 3부터)
```

RRU가 하나 빠지거나 교체돼도 **다른 RRU의 모듈 번호는 밀리지 않게** 하려는 규칙입니다(포인트가 모듈 번호에 묶여 있으므로).

### 0-2. 지금 NCU 보고값을 모듈 2개로 바꿨습니다 (2026-09-11)

```
IsExistModule  [B0][B1] = 00 03   (모듈 1~2)
모듈 1~2 iocategory = 22333333444433
합계: 리더 4 · 입력 16 · 출력 8
```

SSC-324 본체와 같은 모양이라 현장 SSC-324와 나란히 비교할 수 있습니다.
**설정 가져오기를 한 번 다시 해 주세요** — 이전에 만든 모듈 3·4 포인트가 남아 있을 수 있습니다.

### 0-3. DM과 끊겨도 독자 운영하고 이벤트를 몇백만 건 보관합니다

- DM 연결이 끊겨도 NCU는 출입 판정을 계속하고, 이벤트를 저장해 두었다가 다시 연결되면 올립니다
- 보관 목표 **1,000만 건** (SSC-332/334 매뉴얼 사양 `Event : 1,000,000` 보다 넉넉히)

### ⚠ 0-4. 현장 SSC-324에는 SSC-R4(확장 보드)가 없습니다

그래서 **모듈 3 이상, SSC-R4 끊김** 같은 확장 동작은 실장비로 잴 수 없습니다.
아래 질의 중 그런 항목은 **서버·DM 파서가 어떻게 처리하는지** 기준으로 답해 주시면 됩니다. 실측이 가능한 항목은 따로 표시했습니다.

---

## 질의 1. 모듈 번호가 **띄어져** 와도 됩니까? (서버 처리 기준)

RRU 번호를 건너뛰어 설치하면(예: RRU 1, 3만 설치) 모듈 번호가 띄어집니다.

```
IsExistModule [B0][B1] = 00 33   → 모듈 1·2·5·6 존재, 3·4 비어 있음
```

- 1-1. 서버가 포인트를 **모듈 1·2·5·6** 으로 정상 생성합니까? (`devicestructvtwo_tbl.devmoduleenabled = 11001100000000`)
- 1-2. 모듈 5·6의 이벤트가 정상 표시됩니까?
- 1-3. 문제가 있다면 **"번호를 건너뛰지 말 것"** 을 설치 규칙으로 두겠습니다. 어느 쪽인지 알려 주세요

---

## 질의 2. 운영 중 RRU가 빠질 때 — `IsExistModule`을 바꿔도 됩니까? (서버 처리 기준)

RRU-M2 한 대(모듈 3·4)의 USB가 끊기면 NCU는 **USB 단절 이벤트 `0x20030102`** 를 올립니다(회신대로, 복구 이벤트는 없음).
그와 별도로 Device Status를 어떻게 보고할지 정해야 합니다.

```
(A) IsExistModule 에서 모듈 3·4 bit 를 내린다     00 0F → 00 03 → (복구) 00 0F
(B) IsExistModule 은 설치 구성대로 유지하고, 모듈 3·4 의 IO 상태(하위 니블)만 바꾼다
```

- 2-1. (A)로 하면 서버가 **포인트를 지우거나 설정을 다시 요구**합니까? 복구 후 포인트가 그대로 돌아옵니까?
- 2-2. (B)로 할 때 끊긴 모듈을 나타낼 **IO 상태 값**(iocategory 하위 니블)이 규약에 있습니까?
- 2-3. 서버·Platinum 화면이 **모듈 존재/부재를 표시**하는 곳이 있습니까?

---

## 질의 3. 이벤트 주소 필드 — 원시 바이트 값 (🟢 실측 가능)

이벤트 Info(36byte)의 `Module Address`(byte 6), `Reader Address`(byte 7)에 **실제로 어떤 숫자가 들어가는지** 확인 부탁드립니다.
회신의 표기 `(1,13)` 이 원시 바이트로도 `01 0D` 인지, 0부터 세어 `00 0C` 인지에 따라 NCU 구현이 달라집니다.

- 3-1. SSC-324 모듈1 슬롯13(알람) 이벤트의 byte 6·7 원시값
- 3-2. SSC-324 모듈2 **리더 2**(카드 태그) 이벤트의 byte 6·7 원시값
- 3-3. USB 단절 이벤트 `0x20030102`는 RRU-M2 하나(모듈 2개)가 끊겼을 때
  **(가) 모듈마다 1건씩 2건** / **(나) 첫 모듈로 1건** 중 어느 쪽이 서버·화면 해석에 맞습니까? 이때 Reader Address는 무엇을 넣습니까? (서버 처리 기준)

---

## 질의 4. 저장된 이벤트 처리 — SSC-324는 어떻게 합니까? (🟢 대부분 실측 가능)

DM 소스(`clsDevEvent.cs`)를 보면 이미 관련 기능이 있습니다. **SSC-324가 실제로 어떻게 답하는지**를 그대로 따르려 합니다.

| 기능 | DM 소스에서 확인한 것 |
|---|---|
| 한 응답에 이벤트 여러 건 | `GetDevEventInfoAll` — 데이터 길이를 이벤트 크기로 나눠 **여러 건을 받는다** |
| EventCountCheck | 응답 4byte = 저장 건수 (`GetDevEventCountValue`) |
| EventIndexChange | `FromDateTime`(날짜부터) / `FromStartToEnd`(기간) |
| EventReset | 1byte 응답 |

- 4-1. DM과 끊겼다가 다시 연결됐을 때 SSC-324는 **History 응답 하나에 이벤트를 몇 건씩** 싣습니까? DM 쪽 한 응답의 **최대 건수·최대 길이** 제한이 있습니까?
- 4-2. EventCountCheck의 건수는 **저장된 전체** 입니까, **아직 안 올린 것** 입니까?
- 4-3. EventIndexChange를 받으면 SSC-324는 그 시점부터 **이미 올린 이벤트도 다시** History로 올립니까? 끝나면 원래 위치로 돌아갑니까?
- 4-4. EventReset은 **저장 이벤트 전부 삭제** 입니까, **전송 위치만 초기화** 입니까?
- 4-5. 저장소가 가득 차면 SSC-324는 **오래된 것부터 덮어씁니까?** 아직 안 올린 이벤트도 덮어씁니까?
- 4-6. 오래 끊겼다가 **수십만 건**이 한꺼번에 올라올 때 DM·Platinum 쪽에 문제(처리 지연, DB 부하, 화면 폭주)가 있습니까?
  있다면 NCU가 지켜야 할 속도 제한을 알려 주세요

---

## 질의 5. 입력 설정(용도 · 반응 출력)을 받는 형식 (🟢 실측 가능)

회신에서 **입력 용도가 이벤트 코드를 정하고, 화재는 반응 출력 주소로 올라온다**는 것이 확인됐습니다.
그래서 NCU도 DM이 내려 주는 **입력 설정을 저장**해 두어야 합니다.

DM 소스에서 확인한 것:
- `clsDevInputInfo` — `ApplicationType`, `ActiveType`, `DelayTime`, `ShuntStart/EndHourMin`, `LineFault`, **`ReactionModuleAddress`, `ReactionDeviceAddress`**
- 명령 `InputSensorSettingChange` / `InputSensorSettingCheck` (설명: `Active Setting|Active Type|Delay Time|Limit Time|Line Fault|Reaction Setting`)

- 5-1. 이 명령의 **Command / SubCommand / Object 값과 데이터 배치**(바이트 순서·길이)를 알려 주세요.
  가능하면 **SSC-324와 주고받은 원시 바이트 한 쌍**(요청·응답)이 가장 확실합니다
- 5-2. 입력 하나씩 보냅니까, 모듈 단위로 묶어 보냅니까?
- 5-3. `ReactionModuleAddress` / `ReactionDeviceAddress` 도 0부터 셉니까, 1부터 셉니까? (질의 3과 같은 이유)

---

## 질의 6. 기존 대기 항목 · 문서 정리

- 6-1. **화재 이벤트 주소 재측정 결과** (앞선 회신에서 반응 출력을 바꿔 다시 재기로 한 것).
  반응 출력이 **설정되지 않은** 화재 입력은 어떤 주소로 올라옵니까?
- 6-2. **`IsExistModule` 바이트 순서** 서버 실측 — 지금 NCU는 `00 03`(모듈 1~2)을 보냅니다. 서버 표가 `11000000000000` 이 되는지
- 6-3. SSC-324 모듈의 **실제 `mType` 값** — NCU는 지금 `123`(RXM_132)을 보냅니다
- 6-4. SSC-324가 **MultiLanguage 확인**에 답하는 값 — NCU는 지금 `English(1)` 을 보냅니다
- 6-5. `NCU_장치구조_프로토콜_안내.md` 정리 부탁
  - 4절 *"SSC-324 에서 슬롯 14 가 한 번도 안 올라온다 — 확인 중"* 과 7절 🔴1-1 — 회신 문서에서 **용도가 `0` 이었기 때문**으로 풀렸으니 정리
  - 7절 0 · 8절의 **모듈 4개** 기준 표 → 지금은 **모듈 2개** (`devmoduleenabled 11000000000000`, 포인트 리더 4 · 입력 16 · 출력 8)

---

## 질의 7. 사용자 등록 한도 (🟢 일부 실측 가능)

매뉴얼은 `USER : 130,000 / 260,000`(SSC-332 / SSC-334)이고, SD 카드 이미지의 사용자 파일도 262,144칸으로 잡혀 있습니다.
DM 소스에서 `UserBinDataObjectCountMax_SSC` / `_SSCFP` 로 인원을 검사하는 곳은 찾았지만 값의 정의는 찾지 못했습니다.

- 7-1. **현장 SSC-324 펌웨어의 실제 사용자 한도**는 몇 명입니까? (`UserBinDataObjectCountMax_SSC` 값)
- 7-2. 한도를 넘게 내려보내면 **DM이 막습니까, 장비가 거부합니까?** 장비 거부라면 어떤 응답입니까?
- 7-3. 지금 현장은 지문을 쓰지 않는 것으로 압니다. 추후 **지문·안면 인식**을 도입하면 DM이 데이터를 받아 NCU로 보내고,
  NCU가 **별도 망의 생체 장치로 전달**할 계획입니다. **안면 데이터는 V2 규약에서 어떤 명령·구조로 보냅니까?**
  (`User Image 16+N`인지, 별도 명령인지) 지문은 `User Finger 420 × N`으로 이해하고 있습니다
- 7-4. 전체 사용자 다운로드는 **`clsDevUserBin` 바이너리 전송**(총 크기·총 인원·프레임 인덱스)으로만 합니까, 1명씩 보내는 명령도 씁니까?

---

## 참고 — 이미 정한 것

| 항목 | 내용 |
|---|---|
| 장치 종류 | `DeviceCategory=3`(Controller), `DeviceType=33`(SSC_324), Protocol V2 |
| 모듈 모양 | 모듈마다 `22333333444433` (리더 2 · 입력 6 · 출력 4 · 알람 1 · 화재 1) |
| 알람 | `0x18010306` / `0x18010307` (용도 0·2) |
| 화재 | `0x18010401` / `0x18010402` (용도 3) — 주소는 반응 출력 (재측정 대기) |
| USB 단절 | `0x20030102`, 복구 이벤트 없음 |
| LCD 명령 4종 | 성공으로 답하고 버림 |
| 화재 시 전체 개방 | DM 정책 → 컨트롤러마다 Object 206 강제 개방 |

---

## 회신 양식

| 질의 | 답 |
|---|---|
| 1-1·1-2. 띄어진 모듈 번호(1·2·5·6) 포인트 생성·이벤트 표시 | |
| 1-3. 번호 건너뛰기 금지 규칙이 필요한가 | |
| 2-1. 운영 중 `IsExistModule` bit를 내렸다 올리면 서버 동작 | |
| 2-2. 끊긴 모듈을 나타낼 IO 상태 값 | |
| 2-3. 모듈 존재/부재 표시 화면 | |
| 3-1. 모듈1 슬롯13 알람 이벤트 byte 6·7 원시값 | |
| 3-2. 모듈2 리더2 카드 이벤트 byte 6·7 원시값 | |
| 3-3. USB 단절 이벤트: 모듈마다 2건 / 첫 모듈 1건, Reader Address 값 | |
| 4-1. 재연결 후 응답 하나당 이벤트 건수, DM 최대 제한 | |
| 4-2. EventCountCheck 건수의 의미 | |
| 4-3. EventIndexChange 후 동작 | |
| 4-4. EventReset 동작 | |
| 4-5. 저장소 가득 참 동작 | |
| 4-6. 수십만 건 일괄 업로드 시 문제·속도 제한 | |
| 5-1. 입력 설정 명령 Command/SubCommand/Object, 데이터 배치 (원시 바이트) | |
| 5-2. 입력 단위 / 모듈 단위 | |
| 5-3. Reaction 주소 0/1 기준 | |
| 6-1. 화재 주소 재측정 결과, 반응 출력 미설정 시 주소 | |
| 6-2. `IsExistModule` `00 03` 서버 표 결과 | |
| 6-3. SSC-324 `mType` | |
| 6-4. SSC-324 MultiLanguage 확인 응답 값 | |
| 7-1. SSC-324 사용자 한도 | |
| 7-2. 한도 초과 시 동작 | |
| 7-3. 안면 데이터 전송 명령·구조 (지문은 `User Finger 420×N`이 맞는지) | |
| 7-4. 사용자 다운로드 방식 (바이너리 일괄 / 1명씩) | |
